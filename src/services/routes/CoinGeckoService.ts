import { NextFunction, Request, Response } from 'express';
import { TaskT } from '@craigmiller160/ts-functions/types';

export const queryCryptoGecko = (
	req: Request,
	res: Response,
	next: NextFunction
): TaskT<void> => {};
