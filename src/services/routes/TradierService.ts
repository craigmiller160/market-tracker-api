import { NextFunction, Request, Response } from 'express';
import { restClient } from '../RestClient';

export const queryTradier = (req: Request, res: Response, next: NextFunction) => {
    // Finish this
}